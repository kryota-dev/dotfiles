name: Setup PR
on:
  pull_request:
    branches: [main]
    types: [opened]

permissions: {}

jobs:
  auto-assign:
    permissions:
      pull-requests: write
    uses: kryota-dev/actions/.github/workflows/auto-assign-pr.yml@aaf45e6452e1e6bdeee70730be0da0cbad2c9bd0 # v0.1.1
    with:
      bot-assignees: "kryota-dev"
