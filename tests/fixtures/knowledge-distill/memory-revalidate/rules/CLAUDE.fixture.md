# プロジェクト固有の指示（fixture）

陰性対照用のルールファイル。memory 側のどのファイルとも重複しない話題だけを書く。

## ブランチ運用

作業は必ずワークツリーで行い、main を直接変更しない。ブランチ名は `feat/` `fix/` `refactor/`
のいずれかで始める。マージはレビューが揃ってから人が行う。

## エディタ設定

インデントは 2 スペース。行末の空白は保存時に除去する。ファイル末尾には改行を 1 つ置く。
