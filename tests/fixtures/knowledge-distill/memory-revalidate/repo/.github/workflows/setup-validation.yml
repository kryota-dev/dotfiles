# fixture 用のダミーワークフロー。リポジトリ直下ではないため GitHub からは実行されない。
name: Setup Validation (fixture)
on:
  workflow_dispatch:
jobs:
  macos:
    runs-on: macos-latest
    steps:
      - run: echo macos
  ubuntu:
    runs-on: ubuntu-latest
    steps:
      - run: echo ubuntu
