## ユーザーに判断を求めるとき

**判断を仰ぐときは必ず `AskUserQuestion` を使う。** 通常メッセージの末尾に問いを書いて待たない
（平文の問いは報告に埋もれ、選択肢と含意も並べられない）。確認だけの短い問いも例外にしない。
「進めてよろしいですか」と書きかけたら、それは `AskUserQuestion` の呼び出しである。
**承認された恒久対処は、他の作業より先に実行する**（後回しにすると同じ違反をその場で繰り返す）。
